{
  "name": "finance",
  "version": "1.0.0",
  "description": "landing",
  "main": "index.js",
  "scripts": {
    "sass": "sass --watch scss/:css/",
    "sync": "browser-sync start --server --files css/*.css, ./*.html, js/*.js, pages/*.html, data/*.js" 
  },
  "author": "Smilya",
  "license": "ISC"
}
